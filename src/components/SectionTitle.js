const SectionTitle = ({ title }) => {
	return (
      <h2
         className={`uppercase text-2xl md:text-3xl text-[#0B3558] text-center mb-10 font-semibold`}
      >
         {title}
      </h2>
   );
};

export default SectionTitle;
